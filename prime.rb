# Add  code here!
# natural number
# divisible by exactly 2 natural numbers
#   itself 
#   one

def prime(num) 
  if (num >= 2) 
    if (num % 1 == 0) && (num % num == 0)
    TRUE 
  else 
  else FALSE